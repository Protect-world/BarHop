# 地图标记点击优化 + 骨架屏 + 全局错误处理

## Context

**任务1：地图标记点击无反应问题**
当前 [index.js:227](file:///e:/coding/selfCoding/BarHop/miniprogram/pages/index/index.js#L227) `createMarkers(sortedBars)` 为**所有**搜索到的酒吧（可能 50+）创建 markers，但 [index.js:233](file:///e:/coding/selfCoding/BarHop/miniprogram/pages/index/index.js#L233) `displayBars` 最多展示 20 条。当 marker 的 `barIndex >= displayedCount`（如点击第 25 个 marker）时，[index.js:520-525](file:///e:/coding/selfCoding/BarHop/miniprogram/pages/index/index.js#L520) 设置的 `scrollToBarId = bar-24` 在 DOM 中不存在 → 滚动失败，无任何反馈，用户觉得"点了没反应"。

**任务2：需求 13 骨架屏 + 需求 14 全局错误处理**
- [app.js](file:///e:/coding/selfCoding/BarHop/miniprogram/app.js) 没有 `onError`/`onUnhandledRejection`/`onPageNotFound` 全局错误捕获
- [request.js:114-126](file:///e:/coding/selfCoding/BarHop/miniprogram/utils/request.js#L114) 有重试机制但无统一错误提示，各页面各自处理 toast
- 现有 loading 用全屏 `.loading-overlay` 遮罩，体验生硬（白屏 → 突然出现数据）

## 用户决策

| 决策项 | 选择 |
|---|---|
| markers 数量 | 保留全部 markers + 弹窗（超出 20 条的酒吧用底部弹窗显示） |
| 点击行为 | 滚动 + 高亮（临时高亮 2 秒） |
| 骨架屏范围 | 首页 + 详情页 + 收藏页 |
| 网络提示形式 | 顶部横幅 |

附加要求：弹窗设计要美观，避免生硬的线条感。

---

## 任务1 实现方案

### 1.1 修改 `onMarkerClick` 三层处理逻辑

**文件**：`miniprogram/pages/index/index.js` 的 `onMarkerClick` 函数（L512-L527）

三种场景：
- **场景A：`barIndex < displayedCount`**（在已展示列表中）→ 直接滚动 + 高亮
- **场景B：`displayedCount <= barIndex < 20`**（在 20 条内但未加载）→ 扩展 `displayBars` 到 `barIndex+1`，再滚动 + 高亮
- **场景C：`barIndex >= 20`**（超出 20 条上限）→ 显示底部弹窗，展示酒吧简要信息

```js
onMarkerClick: function (e) {
  const markerId = e.markerId;
  if (markerId === 9999) return;  // 用户位置标记

  const marker = this.data.markers.find(m => m.id === markerId);
  if (!marker || marker.barIndex === undefined) return;

  const { barIndex } = marker;
  const { displayedCount, allBars } = this.data;

  if (barIndex < displayedCount) {
    // 场景A：在已展示列表中
    this.scrollToBar(barIndex);
  } else if (barIndex < 20) {
    // 场景B：在 20 条内但未加载 → 扩展后滚动
    const newCount = barIndex + 1;
    this.setData({
      displayBars: allBars.slice(0, newCount),
      displayedCount: newCount
    }, () => this.scrollToBar(barIndex));
  } else {
    // 场景C：超出 20 条 → 显示底部弹窗
    const bar = allBars[barIndex];
    if (bar) {
      this.setData({ popupBar: bar, showBarPopup: true });
    }
  }
},

scrollToBar: function (barIndex) {
  this.setData({
    activeBarIndex: barIndex,
    scrollToBarId: `bar-${barIndex}`
  });
  // 2 秒后取消高亮
  if (this._highlightTimer) clearTimeout(this._highlightTimer);
  this._highlightTimer = setTimeout(() => {
    if (this.data.activeBarIndex === barIndex) {
      this.setData({ activeBarIndex: -1 });
    }
  }, 2000);
},

hideBarPopup: function () {
  this.setData({ showBarPopup: false });
},

goToDetailFromPopup: function () {
  const bar = this.data.popupBar;
  if (bar) {
    this.setData({ showBarPopup: false });
    wx.navigateTo({ url: `/pages/detail/detail?id=${bar.id}` });
  }
}
```

### 1.2 `data` 字段新增

```js
data: {
  // ... 现有 ...
  activeBarIndex: -1,        // 已存在
  popupBar: null,            // 新增：弹窗展示的酒吧
  showBarPopup: false        // 新增：弹窗显示状态
}
```

### 1.3 `index.wxml` 添加底部弹窗

在 `<scroll-view>` 之后、页面最外层 `</view>` 之前添加：

```html
<!-- 地图标记点击弹窗：超出 20 条的酒吧 -->
<view class="bar-popup-mask" wx:if="{{showBarPopup}}" bindtap="hideBarPopup">
  <view class="bar-popup" catchtap="">
    <view class="popup-handle"></view>
    <view class="popup-header">
      <text class="popup-title">{{popupBar.name}}</text>
      <text class="popup-close" bindtap="hideBarPopup">✕</text>
    </view>
    <view class="popup-rating-row" wx:if="{{popupBar.display_rating}}">
      <text class="popup-rating-star">★</text>
      <text class="popup-rating-num">{{popupBar.display_rating}}</text>
      <text class="popup-rating-tag" wx:if="{{popupBar.rating_source === 'user'}}">用户评分</text>
    </view>
    <view class="popup-info-list">
      <view class="popup-info-item">
        <text class="popup-info-icon">📍</text>
        <text class="popup-info-text">{{popupBar.address || '暂无地址'}}</text>
      </view>
      <view class="popup-info-item">
        <text class="popup-info-icon">🚶</text>
        <text class="popup-info-text">距您 {{popupBar.distance}}m</text>
      </view>
      <view class="popup-info-item" wx:if="{{popupBar.tags}}">
        <text class="popup-info-icon">🏷️</text>
        <text class="popup-info-text">{{popupBar.tags}}</text>
      </view>
    </view>
    <view class="popup-btn" bindtap="goToDetailFromPopup">
      <text>查看详情</text>
      <text class="popup-btn-arrow">›</text>
    </view>
  </view>
</view>
```

### 1.4 `index.wxss` 添加弹窗样式（优美、无生硬线条）

设计要点：
- 顶部圆角 + 渐变背景，柔和的阴影
- 顶部小把手（popup-handle）增加层次感
- 信息项之间用极淡分隔色（rgba 0.05），避免硬线条
- 入场动画：从底部上滑

```css
.bar-popup-mask {
  position: fixed;
  top: 0; left: 0; right: 0; bottom: 0;
  background: rgba(0, 0, 0, 0.5);
  z-index: 1000;
  display: flex;
  align-items: flex-end;
  animation: fadeIn 0.2s ease;
}

.bar-popup {
  width: 100%;
  background: linear-gradient(180deg, #1A1A2E 0%, #16213E 100%);
  border-radius: 32rpx 32rpx 0 0;
  padding: 24rpx 40rpx calc(40rpx + env(safe-area-inset-bottom));
  box-shadow: 0 -8rpx 32rpx rgba(0, 0, 0, 0.3);
  animation: slideUp 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

@keyframes slideUp {
  from { transform: translateY(100%); }
  to { transform: translateY(0); }
}

@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

.popup-handle {
  width: 64rpx;
  height: 8rpx;
  background: rgba(255, 255, 255, 0.15);
  border-radius: 4rpx;
  margin: 0 auto 24rpx;
}

.popup-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 20rpx;
}

.popup-title {
  font-size: 36rpx;
  font-weight: 700;
  color: #FFFFFF;
  flex: 1;
  margin-right: 20rpx;
  line-height: 1.4;
}

.popup-close {
  font-size: 32rpx;
  color: rgba(255, 255, 255, 0.5);
  width: 60rpx;
  text-align: right;
}

.popup-rating-row {
  display: flex;
  align-items: center;
  margin-bottom: 24rpx;
  gap: 8rpx;
}

.popup-rating-star {
  color: #FFD700;
  font-size: 28rpx;
}

.popup-rating-num {
  color: #FFD700;
  font-size: 32rpx;
  font-weight: 700;
}

.popup-rating-tag {
  font-size: 20rpx;
  color: rgba(255, 255, 255, 0.7);
  background: rgba(255, 255, 255, 0.12);
  padding: 4rpx 12rpx;
  border-radius: 8rpx;
  margin-left: 8rpx;
}

.popup-info-list {
  margin-bottom: 32rpx;
}

.popup-info-item {
  display: flex;
  align-items: center;
  padding: 20rpx 0;
}

.popup-info-item + .popup-info-item {
  border-top: 1rpx solid rgba(255, 255, 255, 0.05);
}

.popup-info-icon {
  font-size: 28rpx;
  margin-right: 16rpx;
  width: 40rpx;
  text-align: center;
}

.popup-info-text {
  font-size: 26rpx;
  color: rgba(255, 255, 255, 0.85);
  flex: 1;
  line-height: 1.5;
}

.popup-btn {
  background: linear-gradient(135deg, #E94560 0%, #c73650 100%);
  color: #FFFFFF;
  text-align: center;
  padding: 28rpx;
  border-radius: 16rpx;
  font-size: 30rpx;
  font-weight: 600;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8rpx;
  box-shadow: 0 8rpx 24rpx rgba(233, 69, 96, 0.3);
}

.popup-btn:active {
  transform: scale(0.98);
}

.popup-btn-arrow {
  font-size: 36rpx;
  margin-top: -4rpx;
}
```

### 1.5 列表项高亮样式

**文件**：`miniprogram/pages/index/index.wxss`

现有 `.bar-card-active` 类（已在 [index.wxml:175](file:///e:/coding/selfCoding/BarHop/miniprogram/pages/index/index.wxml#L175) 使用）。增强样式，添加脉冲动画：

```css
.bar-card-active {
  background: linear-gradient(135deg, rgba(233, 69, 96, 0.15), rgba(233, 69, 96, 0.08)) !important;
  border: 2rpx solid #E94560 !important;
  box-shadow: 0 4rpx 24rpx rgba(233, 69, 96, 0.3) !important;
  transform: scale(1.02);
  transition: all 0.3s ease;
}
```

---

## 任务2 实现方案：骨架屏（需求 13）

### 2.1 首页骨架屏

**文件**：`miniprogram/pages/index/index.wxml`

在 `<view class="list-container">` 内、现有 `<view class="list-header">` 之后插入骨架屏块（条件：`loading && displayBars.length === 0`）：

```html
<view class="list-container">
  <view class="list-header">...</view>
  
  <!-- 骨架屏：首次加载 -->
  <view class="skeleton-list" wx:if="{{loading && displayBars.length === 0}}">
    <view class="skeleton-card" wx:for="{{[1,2,3,4,5]}}" wx:key="*this">
      <view class="skeleton-img shimmer"></view>
      <view class="skeleton-info">
        <view class="skeleton-line shimmer" style="width: 60%; height: 32rpx"></view>
        <view class="skeleton-line shimmer" style="width: 80%; height: 24rpx"></view>
        <view class="skeleton-line shimmer" style="width: 40%; height: 24rpx"></view>
      </view>
    </view>
  </view>
  
  <block wx:if="{{displayBars.length > 0}}">... 现有列表 ...</block>
  ...
</view>
```

同时移除条件 `wx:elif="{{!loading}}"` 的 empty-state（防止骨架屏期间显示空状态）。

### 2.2 详情页骨架屏

**文件**：`miniprogram/pages/detail/detail.wxml`

在 `loading === true` 时用骨架屏替代 loading-overlay（保留 overlay 作为辅助）。在 `<scroll-view>` 内容区添加：

```html
<scroll-view class="detail-scroll" scroll-y>
  <view class="detail-content">
    <!-- 骨架屏 -->
    <view class="skeleton-detail" wx:if="{{loading}}">
      <view class="skeleton-rating-card shimmer"></view>
      <view class="skeleton-info-card shimmer" wx:for="{{[1,2,3]}}" wx:key="*this"></view>
      <view class="skeleton-photos">
        <view class="skeleton-photo shimmer" wx:for="{{[1,2,3]}}" wx:key="*this"></view>
      </view>
    </view>
    
    <!-- 真实内容（loading 完成后） -->
    <block wx:if="{{!loading}}">... 现有内容 ...</block>
  </view>
</scroll-view>
```

### 2.3 收藏页骨架屏

**文件**：`miniprogram/pages/favorites/favorites.wxml`

类似首页骨架卡片：

```html
<view wx:else class="bar-list">
  <!-- 骨架屏 -->
  <view class="skeleton-list" wx:if="{{loading}}">
    <view class="skeleton-card" wx:for="{{[1,2,3,4]}}" wx:key="*this">
      <view class="skeleton-img shimmer"></view>
      <view class="skeleton-info">
        <view class="skeleton-line shimmer" style="width: 60%; height: 32rpx"></view>
        <view class="skeleton-line shimmer" style="width: 80%; height: 24rpx"></view>
        <view class="skeleton-line shimmer" style="width: 40%; height: 24rpx"></view>
      </view>
    </view>
  </view>
</view>
```

### 2.4 骨架屏通用样式（三个页面共用模式）

**文件**：各页面 wxss（首页/详情页/收藏页），骨架样式统一：

```css
/* shimmer 闪烁动画 */
.shimmer {
  background: linear-gradient(
    90deg,
    rgba(255, 255, 255, 0.04) 0%,
    rgba(255, 255, 255, 0.12) 50%,
    rgba(255, 255, 255, 0.04) 100%
  );
  background-size: 200% 100%;
  animation: shimmer 1.5s infinite;
  border-radius: 8rpx;
}

@keyframes shimmer {
  0% { background-position: 200% 0; }
  100% { background-position: -200% 0; }
}

/* 骨架卡片 */
.skeleton-list {
  padding: 20rpx 0;
}

.skeleton-card {
  display: flex;
  background: rgba(255, 255, 255, 0.03);
  border-radius: 16rpx;
  padding: 20rpx;
  margin-bottom: 20rpx;
}

.skeleton-img {
  width: 180rpx;
  height: 180rpx;
  border-radius: 12rpx;
  flex-shrink: 0;
}

.skeleton-info {
  flex: 1;
  padding-left: 24rpx;
  display: flex;
  flex-direction: column;
  justify-content: center;
  gap: 16rpx;
}

.skeleton-line {
  border-radius: 8rpx;
}

/* 详情页专用骨架 */
.skeleton-rating-card {
  height: 200rpx;
  margin-bottom: 24rpx;
  border-radius: 16rpx;
}

.skeleton-info-card {
  height: 80rpx;
  margin-bottom: 16rpx;
  border-radius: 12rpx;
}

.skeleton-photos {
  display: flex;
  gap: 12rpx;
}

.skeleton-photo {
  width: 200rpx;
  height: 200rpx;
  border-radius: 12rpx;
}
```

> 因项目暂无全局 wxss，骨架样式会写入各页面 wxss（重复但隔离）。

---

## 任务2 实现方案：全局错误处理（需求 14）

### 3.1 `app.js` 添加全局错误捕获 + 网络监听

**文件**：`miniprogram/app.js`

新增：
- `onError` / `onUnhandledRejection` / `onPageNotFound` 全局回调
- 网络状态监听 `initNetworkMonitor()`
- `globalData` 添加 `isConnected`、`networkType`
- 网络状态变化监听器列表 `networkListeners` 和注册方法 `onNetworkChange`

```js
App({
  onLaunch: function () {
    // ... 现有登录逻辑 ...
    this.initNetworkMonitor();
  },

  onError: function (err) {
    console.error('[App] 全局错误:', err);
  },

  onUnhandledRejection: function (res) {
    console.error('[App] 未处理的 Promise 拒绝:', res.reason);
  },

  onPageNotFound: function () {
    wx.switchTab({ url: '/pages/index/index' });
  },

  initNetworkMonitor: function () {
    wx.getNetworkType({
      success: (res) => {
        this.globalData.networkType = res.networkType;
        this.globalData.isConnected = res.networkType !== 'none';
      }
    });
    wx.onNetworkStatusChange((res) => {
      this.globalData.networkType = res.networkType;
      this.globalData.isConnected = res.isConnected;
      this.networkListeners.forEach(cb => {
        try { cb(res); } catch (e) { console.error('[App] 网络监听器错误:', e); }
      });
    });
  },

  onNetworkChange: function (callback) {
    this.networkListeners.push(callback);
    // 立即返回当前状态
    callback({
      isConnected: this.globalData.isConnected,
      networkType: this.globalData.networkType
    });
  },

  offNetworkChange: function (callback) {
    const idx = this.networkListeners.indexOf(callback);
    if (idx > -1) this.networkListeners.splice(idx, 1);
  },

  networkListeners: [],

  globalData: {
    // ... 现有 ...
    networkType: 'unknown',
    isConnected: true
  }
});
```

### 3.2 新建 `utils/errorHandler.js`

**文件**：`miniprogram/utils/errorHandler.js`

统一错误分类与提示：

```js
function showErrorToast(message) {
  wx.showToast({
    title: message || '操作失败',
    icon: 'none',
    duration: 2000
  });
}

function handleApiError(err, customMessage) {
  console.error('[API Error]:', err);
  
  if (!err) {
    showErrorToast(customMessage || '未知错误');
    return;
  }
  
  // 网络错误
  if (err.code === -1 && err.message && err.message.includes('网络')) {
    showErrorToast('网络异常，请检查网络连接');
    return;
  }
  
  // 认证错误
  if (err.code === 401) {
    showErrorToast('登录已过期，请重新登录');
    return;
  }
  
  // 业务错误
  showErrorToast(err.message || customMessage || '操作失败');
}

module.exports = {
  showErrorToast,
  handleApiError
};
```

### 3.3 修改 `utils/request.js` 集成错误处理

**文件**：`miniprogram/utils/request.js`

新增 `silent` 参数（默认 `false`，需要静默时显式传入 `true`）：

```js
function request(options) {
  const {
    url, method = 'GET', data = {}, header = {},
    needAuth = true, retry = 2, timeout = config.TIMEOUT || 10000,
    deduplicate = false, silent = false  // 新增 silent
  } = options;
  
  // ... 现有逻辑 ...
  
  fail: (err) => {
    // ... 现有重试逻辑 ...
    if (retryCount < retry) {
      retryCount++;
      currentTimeout = timeout * Math.pow(1.5, retryCount);
      console.log(`[Request] 重试 ${retryCount}/${retry}: ${fullUrl}`);
      setTimeout(doRequest, 1000 * retryCount);
    } else {
      const errorMsg = '网络请求失败，请检查网络连接';
      if (!silent) {
        const errorHandler = require('./errorHandler');
        errorHandler.showErrorToast(errorMsg);
      }
      reject({ code: -1, message: errorMsg });
    }
  }
}
```

### 3.4 新建全局网络横幅组件

**目录**：`miniprogram/components/network-banner/`

**network-banner.wxml**：
```html
<view class="network-banner" wx:if="{{!isConnected}}">
  <text class="banner-icon">⚠️</text>
  <text class="banner-text">网络异常，请检查网络连接</text>
</view>
```

**network-banner.wxss**：
```css
.network-banner {
  position: fixed;
  top: 0; left: 0; right: 0;
  background: linear-gradient(135deg, #E94560 0%, #c73650 100%);
  color: #FFFFFF;
  padding: 16rpx 32rpx;
  padding-top: calc(16rpx + env(safe-area-inset-top));
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 9999;
  animation: slideDown 0.3s ease;
  box-shadow: 0 4rpx 16rpx rgba(233, 69, 96, 0.3);
}

@keyframes slideDown {
  from { transform: translateY(-100%); }
  to { transform: translateY(0); }
}

.banner-icon {
  margin-right: 12rpx;
  font-size: 28rpx;
}

.banner-text {
  font-size: 26rpx;
  font-weight: 500;
}
```

**network-banner.js**：
```js
const app = getApp();

Component({
  data: {
    isConnected: true
  },

  lifetimes: {
    attached() {
      this.setData({ isConnected: app.globalData.isConnected });
      this._networkCallback = (res) => {
        this.setData({ isConnected: res.isConnected });
      };
      app.onNetworkChange(this._networkCallback);
    },

    detached() {
      app.offNetworkChange(this._networkCallback);
    }
  }
});
```

**network-banner.json**：
```json
{
  "component": true
}
```

### 3.5 注册并使用网络横幅组件

**`app.json` 全局注册**：
```json
"usingComponents": {
  "network-banner": "/components/network-banner/network-banner"
}
```

**三个页面 wxml 顶部添加**（在 `page-container` 内的第一个元素）：

```html
<view class="page-container">
  <network-banner />
  <!-- 现有内容 -->
</view>
```

涉及文件：
- `miniprogram/pages/index/index.wxml`
- `miniprogram/pages/detail/detail.wxml`
- `miniprogram/pages/favorites/favorites.wxml`
- `miniprogram/pages/review/review.wxml`
- `miniprogram/pages/profile/profile.wxml`

---

## 涉及文件汇总

### 任务1（地图标记优化）
| 文件 | 改动 |
|---|---|
| `miniprogram/pages/index/index.js` | 修改 `onMarkerClick`、新增 `scrollToBar`、`hideBarPopup`、`goToDetailFromPopup`，新增 data 字段 |
| `miniprogram/pages/index/index.wxml` | 新增底部弹窗组件 HTML |
| `miniprogram/pages/index/index.wxss` | 新增弹窗样式、增强 `.bar-card-active` |

### 任务2.1（骨架屏）
| 文件 | 改动 |
|---|---|
| `miniprogram/pages/index/index.wxml` | 新增骨架屏 HTML |
| `miniprogram/pages/index/index.wxss` | 新增 shimmer/skeleton 样式 |
| `miniprogram/pages/detail/detail.wxml` | 用骨架屏包裹现有内容 |
| `miniprogram/pages/detail/detail.wxss` | 新增骨架样式 |
| `miniprogram/pages/favorites/favorites.wxml` | 新增骨架屏 HTML |
| `miniprogram/pages/favorites/favorites.wxss` | 新增骨架样式 |

### 任务2.2（全局错误处理）
| 文件 | 改动 |
|---|---|
| `miniprogram/app.js` | 新增 onError/onUnhandledRejection/onPageNotFound + 网络监听 |
| `miniprogram/app.json` | 全局注册 network-banner 组件 |
| `miniprogram/utils/errorHandler.js` | **新建** |
| `miniprogram/utils/request.js` | 新增 silent 参数 + 失败时调 errorHandler |
| `miniprogram/components/network-banner/*` | **新建** 4 个文件 |
| 5 个页面 wxml | 顶部添加 `<network-banner />` |

---

## 验证方案

### 任务1 验证
1. 启动小程序，进入首页，确保有 20+ 酒吧数据（可通过移动地图位置触发）
2. **场景A**：点击前 10 个 marker 中的一个 → 列表应滚动到对应项并高亮 2 秒
3. **场景B**：手动滚动列表触发 `loadMoreBars` 加载到 20 条后，点击第 15 个 marker（已加载范围内）→ 滚动 + 高亮
4. **场景C**：在地图上找到超出列表的 marker（barIndex >= 20），点击 → 底部弹出弹窗，显示酒吧名/地址/评分/距离，点击"查看详情"跳转详情页
5. 点击弹窗外遮罩或 ✕ 关闭按钮 → 弹窗消失

### 任务2.1 验证（骨架屏）
1. 清除小程序缓存，冷启动
2. 首页加载时应看到 5 个骨架卡片闪烁，数据到达后平滑过渡到真实列表
3. 进入详情页时应看到骨架（评分卡 + 信息卡 + 照片占位），数据到达后过渡
4. 切到收藏页（无缓存时）应看到骨架卡片

### 任务2.2 验证（全局错误处理）
1. **网络横幅**：开启飞行模式 → 顶部出现红色横幅"网络异常，请检查网络连接"；关闭飞行模式 → 横幅消失
2. **请求错误提示**：在弱网环境触发请求失败 → 应看到 toast "网络异常，请检查网络连接"
3. **全局错误**：在控制台执行 `throw new Error('test')` → 控制台有 `[App] 全局错误` 日志（不打扰用户）
4. **404 页面**：访问不存在的页面 → 自动跳转到首页

### 整体验证
- 启动后端：`cd server && npm start`
- 启动小程序：在微信开发者工具中编译运行
- 在真机上测试网络横幅（开发者工具的网络模拟有限）
